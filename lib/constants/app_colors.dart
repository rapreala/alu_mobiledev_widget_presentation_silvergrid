import 'package:flutter/material.dart';

/// Application color constants
/// Spotify-inspired color scheme
class AppColors {
  // Spotify brand colors
  static const Color spotifyGreen = Color(0xFF1DB954);
  static const Color darkBackground = Color(0xFF121212);
  static const Color cardBackground = Color(0xFF181818);
  
  // Text colors
  static const Color primaryText = Colors.white;
  static final Color secondaryText = Colors.white.withValues(alpha: 0.6);
  
  // Shadow color
  static final Color shadow = Colors.black.withValues(alpha: 0.4);
}